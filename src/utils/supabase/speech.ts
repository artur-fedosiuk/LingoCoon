const languagemap = (shortCode: string) => {
    switch(shortCode){
        case 'it': return 'it-IT'
        case 'en': return 'es-US'
        case 'uk': return 'uk-UA'
        case 'fr': return 'fr-FR'
    }
};

export const speak = (text: string, lnguage: string) => {
    
}